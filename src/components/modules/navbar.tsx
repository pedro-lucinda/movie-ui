import { FlexBox } from "@ui5/webcomponents-react";

export const NavBar = () => {
  return (
    <FlexBox
      fitContainer
      id="navbar"
      alignItems="Center"
      justifyContent="Center"
    >
      Header
    </FlexBox>
  );
};
